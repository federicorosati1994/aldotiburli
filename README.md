# Aldo - Parrucchiere per uomo

User Interface of Aldo - Parrucchiere per uomo website

## Getting started

- `npm i` to install dependencies
- `npm start` to launch the dev server on `http://localhost:3000`
- `npm run build`